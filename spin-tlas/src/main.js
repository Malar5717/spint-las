import './style.css'
import { setupGlobe, setTheme } from './globe.js';

const globeContainer = document.querySelector('#globe');
setupGlobe(globeContainer);

// Expose setTheme to HTML
window.setTheme = setTheme;
git@github.com:Malar5717/spint-las.gitgit@github.com:Malar5717/spint-las.gitgit@github.com:Malar5717/spint-las.gitgit pull origin main --allow-unrelated-histories
